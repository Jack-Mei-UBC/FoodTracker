# Normalize line endings so the repo is consistent across Windows/macOS/Linux.
* text=auto eol=lf

# Shell scripts must stay LF or they break under bash (CI runs on Linux).
*.sh text eol=lf

# PowerShell is happy with either; keep it CRLF on Windows checkouts.
*.ps1 text eol=crlf
